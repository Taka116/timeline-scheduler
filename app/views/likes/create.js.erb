$(function() {
    $(document).on("custom:like_button_delete", function() {
       $('#like-button').html("<%= escape_javascript(render(partial: 'like_delete', locals: {user: @user, univ_class: @univ_class})) %>");
    });
});

$(document).trigger("custom:like_button_delete");